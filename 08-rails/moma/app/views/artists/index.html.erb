<h1>All Artists</h1>

<% @artists.each do |artist| %>
    <%= image_tag artist.image if artist.image.present? %>
    <ul>
        <li>Name: <%= artist.name %></li>
        <li>Nationality: <%= artist.nationality %></li>
        <li>Date of birth: <%= artist.dob %></li>
        <li>Period: <%= artist.period %></li>
    </ul>
<% end %>
